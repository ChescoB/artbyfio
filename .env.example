# NextAuth configuration
NEXTAUTH_SECRET="your-random-secret-string-here"

# Admin credentials (optional - only needed if using admin login)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="your-secure-password"

# Email service (Resend API for contact form)
RESEND_API_KEY="re_your_resend_api_key"

# AWS configuration (currently unused - legacy from database setup)
AWS_PROFILE=hosted_storage
AWS_REGION=us-west-2
AWS_BUCKET_NAME=your-bucket-name
AWS_FOLDER_PREFIX=your-prefix/

# Database URL (currently unused - Prisma removed)
DATABASE_URL="postgresql://user:password@host:port/database"

canvas.addEventListener('pointerdown',function(e){if(S.mode!=='play')return;S.drag=true;P.target=pointX(e);canvas.setPointerCapture&&canvas.setPointerCapture(e.pointerId);e.preventDefault()},{passive:false});canvas.addEventListener('pointermove',function(e){if(S.mode!=='play'||!S.drag)return;P.target=pointX(e);e.preventDefault()},{passive:false});canvas.addEventListener('pointerup',function(e){S.drag=false;e.preventDefault()},{passive:false});canvas.addEventListener('pointercancel',function(){S.drag=false});
bind('start',start);bind('again',start);bind('pause',pause);bind('resume',resume);bind('pulse',nova);bind('hangar',function(){show(el.hangar);renderSkins()});bind('hangarBack',function(){show(el.menu);refreshMenu()});bind('overHangar',function(){show(el.hangar);renderSkins()});bind('quit',finish);
document.addEventListener('visibilitychange',function(){if(document.hidden&&S.mode==='play')pause()});addEventListener('resize',resize,{passive:true});resize();refreshMenu();S.last=performance.now();requestAnimationFrame(loop);